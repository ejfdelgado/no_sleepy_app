
- Allow share an alarm to automatically add a new one with some specific name, latitud and longitude.
- multilanguage.
- customize icon and splash.
- almacenar el geocode a unos tres diferentes niveles de precision.

namespace: tv.pais.nosleepygps

.setMinUpdateDistanceMeters(BuildConfig.MIN_DISTANCE_METERS.toFloat() 10)